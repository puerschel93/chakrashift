import { Center } from "@chakra-ui/react";
import { Autocomplete } from "autocomplete";

const Demo = () => {
  return (
    <Center w="100vw" h="100vh">
      <Autocomplete />
    </Center>
  );
};

export default Demo;
